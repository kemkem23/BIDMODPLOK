const API_BASE = 'http://localhost:5000/api';

const VIEWS = {
  TRACK: 'TRACK',
  LEADERBOARD: 'LEADERBOARD',
};

const RACE_STATUS = {
  WAITING: 'waiting',
  RUNNING: 'running',
  FINISHED: 'finished',
};

module.exports = { API_BASE, VIEWS, RACE_STATUS };
